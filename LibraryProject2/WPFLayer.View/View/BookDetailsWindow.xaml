<Window
    x:Class="WPFLayer.View.BookDetailsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPFLayer.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="BookDetailsWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <StackPanel Background="LightYellow">
        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="ID: " />
            <TextBlock FontSize="16" Text="{Binding BookId}" />
        </StackPanel>

        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="Title: " />
            <TextBlock FontSize="16" Text="{Binding Title}" />
        </StackPanel>

        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="Author: " />
            <TextBlock FontSize="16" Text="{Binding Author}" />
        </StackPanel>

        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="Type: " />
            <TextBlock FontSize="16" Text="{Binding Type}" />
        </StackPanel>

        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="Penalty Cost: " />
            <TextBlock FontSize="16" Text="{Binding PenaltyCost}" />
        </StackPanel>

        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="Return Date: " />
            <TextBlock FontSize="16" Text="{Binding ReturnDate}" />
        </StackPanel>

        <StackPanel Margin="3" Orientation="Horizontal">
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="Availability: " />
            <TextBlock FontSize="16" Text="{Binding State}" />
        </StackPanel>
    </StackPanel>
</Window>
